typedef enum {UNDISCOVERED, DISCOVERED, VISITED} VStatus;
typedef enum {UNDETERMINED, TREE, CROSS, BACKWARD, FORWARD} EType;

template <typename Tv, typename Te> class Graph
{
private:

public:
	Graph();
	~Graph();


};

Graph::Graph()
{
}

Graph::~Graph()
{
}